
需求：
1，数字账号从60000到9999999999（类似qq号一样的东东）
2，用户获取数字账号为随机分配，也可递加分配，需要符合如下规则
3，特殊账号需要保留，不能分配给用户，比如：112233（连续两连号），88888（单一数字号），444555（三连号）
4，用户的数字账号不能重复
5，注册时分配数字账号给用户，不能让用户在注册时等待太久
6，考虑性能问题
7，后台可手动创建新用户，并赋予任何数字账号（当然包括特殊账号，已使用的除外）

实现：

方案一：
1，提前生成一批账号保存在数据表里（表名：UserAccount）
2，数据表中需要标记哪些账号是已经使用过的，哪些是未使用的
3，特殊账号不插入数据表（每个规则建一个函数）
4，用户一张表（表名：UserMember），在注册时通过获取表UserAccount中未使用的一个数字账号进行注册
5，后台创建用户时，可直接输入数字账户，后台判断数字账户不在表UserMember中即可注册成功，否则提示数字账号已使用;
创建成功后需要，反查UserAccount中是否有该账号，有需要修改为已使用
6，考虑性能，每次后台插入账号到表UserAccount中时，最多插入10万条
7，后台插入账号到表UserAccount中时，默认获取表UserAccount中最大的值+1做起点插入值，往下加100000作为终点插入值
8，插入账号到表UserAccount之前需要把表中UserMember的数字账号是起点值到终点值的账号读出来，
在插入时需判断值是否已存在UserMember中，存在无需插入
9，后台监控表UserAccount未使用账号情况

方案二：
注册时随机给
会出现空号
